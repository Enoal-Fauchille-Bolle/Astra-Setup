- name: Restart NUT
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
    daemon_reload: true
  loop:
    - nut-driver-enumerator
    - nut-server
    - nut-monitor
  become: true
